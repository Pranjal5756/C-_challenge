#include <iostream>
using namespace std;
int main() {
    // operator is a way to perform logical, arithmetical, assignable and comparable operations 

    // for logical purposes we use logical operators there are some operators of logical which is written below
    // ||, !, &&

    // in arithmetical section we have these operators 
    // +, -, *, /, %, --, ++ 

    // in comparison section we have these operators 
    // == != >	< >= <=

    // in the section of assignable operator we have
    // =	,+=,-=, *=, /=, %=, &=,|=,^=,>>=,<<=

    // { &=,|=,^=,>>=,<<= } I have to pratice this ;
    cout << "operator";


    return 0;
}